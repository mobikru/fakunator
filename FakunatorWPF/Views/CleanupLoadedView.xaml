<UserControl x:Class="Fakunator.Views.CleanupLoadedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Fakunator.ViewModels">

    <Grid Margin="32,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- File info card -->
            <RowDefinition Height="16"/>    <!-- Spacer -->
            <RowDefinition Height="*"/>     <!-- DataGrid preview -->
            <RowDefinition Height="16"/>    <!-- Spacer -->
            <RowDefinition Height="Auto"/>  <!-- Start button -->
        </Grid.RowDefinitions>

        <!-- ── File info card ─────────────────────────────────────── -->
        <Border Grid.Row="0" Style="{StaticResource Card}">
            <DockPanel>
                <!-- File icon badge -->
                <Border DockPanel.Dock="Left" Width="48" Height="48" CornerRadius="10"
                        Margin="0,0,16,0" VerticalAlignment="Center">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#5e6ad2" Offset="0"/>
                            <GradientStop Color="#818cf8" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <TextBlock Text="TXT" Foreground="White" FontWeight="Bold" FontSize="13"
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               FontFamily="{StaticResource MonoFont}"/>
                </Border>

                <!-- File info text -->
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="TxtFileName" FontWeight="SemiBold" FontSize="15"
                               Foreground="{DynamicResource Fg1Brush}"/>
                    <TextBlock x:Name="TxtFileMeta" FontSize="12"
                               Foreground="{DynamicResource Fg3Brush}" Margin="0,3,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- ── Preview DataGrid ───────────────────────────────────── -->
        <Border Grid.Row="2" Background="{DynamicResource Bg2Brush}"
                BorderBrush="{DynamicResource Border1Brush}" BorderThickness="1"
                CornerRadius="12" Padding="0" ClipToBounds="True">
            <DataGrid x:Name="PreviewGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="None"
                      CanUserResizeRows="False"
                      CanUserReorderColumns="False"
                      CanUserSortColumns="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      BorderThickness="0"
                      Background="Transparent"
                      RowBackground="Transparent"
                      AlternatingRowBackground="{DynamicResource Bg3Brush}"
                      Foreground="{DynamicResource Fg1Brush}"
                      HorizontalScrollBarVisibility="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      EnableRowVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">

                <DataGrid.Resources>
                    <!-- Remove focus border -->
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="Foreground" Value="{DynamicResource Fg1Brush}"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="{TemplateBinding Background}"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Row style -->
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource Fg1Brush}"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource Bg4Brush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <!-- Column header style -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource Bg2Brush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource Fg3Brush}"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource Border1Brush}"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="#" Binding="{Binding RowNumber}" Width="60">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource Fg4Brush}"/>
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Header="EMAIL" Binding="{Binding Email}" Width="*">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{DynamicResource Fg1Brush}"/>
                                <Setter Property="FontSize" Value="12.5"/>
                                <Setter Property="FontFamily" Value="{StaticResource MonoFont}"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- ── Start cleanup button ───────────────────────────────── -->
        <Button Grid.Row="4" Content="▶  Запустить очистку"
                Style="{StaticResource PrimaryBtn}"
                Height="42" FontSize="15"
                HorizontalAlignment="Stretch"
                Click="OnStartCleanup"/>
    </Grid>
</UserControl>
